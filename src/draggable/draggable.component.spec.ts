/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TepuyDraggableComponent } from './tepuy-draggable.component';

describe('TepuyDraggableComponent', () => {
  let component: TepuyDraggableComponent;
  let fixture: ComponentFixture<TepuyDraggableComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TepuyDraggableComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TepuyDraggableComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/